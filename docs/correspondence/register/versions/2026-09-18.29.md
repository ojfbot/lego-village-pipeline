---
kind: migrated
note_sha256: e6594ba3aa60a879611197b68dae9e6f91547583a0e6cd7081831422afbe197d
previous_version: 2026-09-18.28
register_version: 2026-09-18.29
slice: s-29
---
At `.29` adds the **CORR-LEGO-PIPE-029-R0 row — Claude Code's delivery debrief for the design-package protocol**, written at the operator's instruction after PR #13 merged, about the implementing agent rather than the artifact. Fifteen findings. On the agent: it shipped a contract asserting fifteen acceptance outcomes satisfied with **zero committed tests**, and four review rounds then found **thirteen defects of one shape** — a guarantee asserted at one layer while the layer beneath went unchecked (prose → interpreter → schemas → annotations → the test written to prove the claim, which had itself rotted and passed on `Ran 0 tests`); **three statements the author made to reviewers were false**, each true of something adjacent to the code; and **one defect of fourteen was self-found**, self-verification reaching exactly one layer less deep than peer review in every round. On the process: the **review gate before implementation was operator-inserted**, not proposed by the agent about to implement; two-reviewer reconciliation held for the plan and **decayed silently to one reviewer** for the implementation; and the implementation review **produced no register rows at all** — six reviews and five replies that changed the schemas, the tools and the merge decision exist only as pull-request comments, with unstable finding identities and **every cited commit hash rewritten by the merge**, so all six citations are unresolvable from `main` (the mapping is preserved in the memo's §5, verified by tree identity). Rule 11 already had the answer: a citation is repository + path + commit + *content* hash, and the exchange used the one component a merge policy may rewrite. §6 is input for the operator's planned pull-request/review schema and deliberately stops short of designing it; §7 sets baselines so that skill can be measured rather than asserted. **Number 029 proposed, and the route there is part of the record:** 027 was claimed in flight by ChatGPT (operator relay 2026-09-19), and **Claude (Cowork) committed its own debrief as 028** in the shared clone while this one was being written — two agents drafting against the same next-free number, minutes apart, to the same operator instruction, neither aware of the other; discovered only when the shared clone's branch moved under the authoring session. Rule 4 absorbed it (a claimed number is not contested) but nothing prevented it: **number allocation has no reservation step another agent can observe**, raised as an open item. The two debriefs are complements — Cowork assesses its conduct as reviewer, 029 assesses Claude Code's as implementer. Next free is **030+** on the assumption 027 and 028 land; if either never does, its number stays consumed (rule 2). **The placeholder rows this delta first carried for 027 and 028 were dropped before landing**: CORR-027's delta records allocations in the next-free row instead, which is the better pattern — a claimed number belongs in the allocation marker, never in a row of its own, or the real row duplicates it when it lands. Recorded because the mistake was this author's and the correction is the peer's. 029 passes `memo_preflight.py` (v2) exit 0 at authoring, register read `.26`. Also recorded, not repaired, from PR #13's approving review: concurrent runs of `tests/test_design_package.py` race over the staged fixture path `tools/_pre_extraction_check.py` — non-blocking, the documented one-command workflow is unaffected, and the head was already approved; it wants a per-run temporary path before any CI runs the suite concurrently. **Landed as train car 3 at `.29`**, assigned by the shepherd against canonical `main` `a732ace` after PR #14 merged at `.28`; this branch took `main` by merge commit, not rebase, so the heads cited in the PR #16 review thread (`e9e0a67`, `3530a70`, `d2ecd8e`, `ea04ab4`) stay reachable — the pin-durability failure recorded in this very memo's R-10 is not repeated by the act of landing it. The delta first claimed `.27` while three memos were in flight; renumbered on landing per the procedure recorded at `.26`, and no memo content or citation moved. 