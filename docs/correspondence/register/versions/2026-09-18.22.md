---
kind: migrated
note_sha256: adb2ea450cd453ff8806b2d5449960ab5596cc04091680892c8862dc5917404d
previous_version: 2026-09-18.21
register_version: 2026-09-18.22
slice: s-22
---
At `.22` `.22` adds the 023-R2 row and marks R1 superseded (the R2 branch was cut from `.19` and claimed `.20`; renumbered to `.22` on rebase, as the `.21` note anticipated). R2 folds in the operator's ten rulings on R1 §10 (2026-09-18): thread renamed **`drafting-table`** (rule 16: `app-stack` retired — it was never confirmed; the R0/R1 rows keep it as recorded); diving vocabulary replaced by LEGO learning-through-play and set-building language (bag · booklet · picture on the box · step back · real bricks); fixture rule F5 (real-world state progressing in ambiguity · fidelity · autonomy; one village shared by prototypes and app); design cuts weekly on Fridays; Ladle; `board.json` only, no GitHub Projects; ChatGPT at rung L1; the morning-cockpit question becomes a Claude Code research item that also captures the cluster onboarding pipeline. Two items stay open, both on the design package's identity (ARCHITECTURE §1a), needed by cut R2 on 25 Sep. R2 passes both validators (`tools/preflight.py` v1 and `tools/memo_preflight.py` per `.20`). `.22` also repairs doubled version labels ("At `.n` `.n`") left in the `.15`–`.19` notes by a bump script. 