---
affected_memos:
- CORR-LEGO-PIPE-030-R0
- CORR-LEGO-PIPE-031-R0
- HANDOFF-LEGO-PIPE-032-R1
allocations_consumed:
- 032-R1
finalized_from_main: 582fb63d63c3f9188516faade67fd6a8116287a3
kind: finalized
note_sha256: ea503171c3c4aa9182216c91e64e58276143958a7bde6280379d896f7eeb713c
previous_version: 2026-09-18.31
register_version: 2026-09-18.32
---
Lands the register-shape migration specified by **HANDOFF-LEGO-PIPE-032-R1** (operator-ratified 2026-09-20, PR #21 decision docket comment 5752476543; accepted R0 head `48bcbf50`, R1 head `3a1729c8`), executed from the all-`main` freeze base `582fb63d63c3f9188516faade67fd6a8116287a3` (register `.31`). The 39,268-byte version line is split mechanically into thirty immutable records under `register/versions/` — twenty-four with a raw slice of their own (`.8`–`.31`) and six (`.2`–`.7`, Q-11 option a) resolving into the `.8` slice's `Superseded versions:` enumeration, each with an anomaly entry — proved byte-exact by `register/MIGRATION-582fb63d63c3f9188516faade67fd6a8116287a3.yaml` (23 accepted and 1 rejected seam candidate, the quoted `At `.22`` inside the `.24` note at byte 26,240). `register/ALLOCATIONS.yaml` is seeded from the table's first column plus the Q-01 reservations (032 in flight, 033/034/035 reserved); next free is derived. `register/KNOWN-ANOMALIES.yaml` carries the doubled `.22` label (and the `.24` note that quotes it) and the six no-slice versions as frozen data. Tools: `tools/register_migrate.py`, `tools/register_lint.py` (RL-01…RL-18, offline and `--git` tiers), `tools/register_finalize.py` (RF-01…RF-06), `tools/schemas/register-version.v1.schema.json`, `tests/test_register.py`, `.github/workflows/register-lint.yml`. Policy changes ratified by the docket and applied here: one version per accepted landing (Q-02), assigned only at finalization (Q-03), one immutable file per version (Q-04), the enumerated authority set (Q-05), Rule 18 merge-method enforcement by repository setting (Q-06), `register-lint` as a required check (Q-09), the per-commit bump sentence retired in `REGISTER.md`, `AGENTS.md` and `CLAUDE.md` (Q-10). Rows changed: 032-R1 added (allocated by James 2026-09-20; R0 superseded, bytes retained), 030-R0 and 031-R0 marked accepted as amended by 032-R1 (Q-13), next-free row now derived from the ledger; every other correspondence and instruments row byte-identical (RL-13/RL-14). This version is the first real `register_finalize.py` run (Q-12); the canary landing is the second.
