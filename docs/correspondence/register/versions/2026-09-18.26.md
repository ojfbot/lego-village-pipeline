---
kind: migrated
note_sha256: 7ab4812afc41a1505cc98a260616f2e85882a93d1374c1b128fca698757d2489
previous_version: 2026-09-18.25
register_version: 2026-09-18.26
slice: s-26
---
At `.26` lands the **design-package protocol as built — HANDOFF-LEGO-PIPE-024-R1 supersedes R0** (rule 6; R0's bytes stay on disk — both reviews cite content sha256 `e339867f…f308a`), by PR `feat/design-package-contract`, **James merges — his merge ratifies the schema corrections he delegated to Claude Code on 2026-09-19** (the five converged schema findings both reviews agreed on). What lands: canonical schemas `tools/schemas/design-package.v1.schema.json` (designer manifest: cut-key triple, `governing_brief`+`answers[]`, qualified digest-bound `supersedes`, `maturity`/`tier_coverage` split, `mock_math` required-boolean, typed references, scoped defects), `design-package-import.v1.schema.json` (importer receipt: normative `tree_sha256` spec, nullable `archive_sha256`+status, `disposition imported|rejected|recorded_not_imported`, authority-naming waivers), `design-package-overlay.v1.schema.json` (reconstruction overlay — a distinct type bound to `{package_path, tree_sha256}` with per-field evidence strength; `path_prefix_strip` valid only there); the schema interpreter extracted to `tools/schema_lint.py` and shared by `memo_preflight.py` + the new `tools/package_preflight.py` (full memo-corpus regression byte-identical; frozen v1 `preflight.py` untouched); `tools/package_drift.py` (file-level change evidence, per-cell evidence methods, DEC-id ledger gate) and `tools/design_pkg.py` (digest + generated inventory); `docs/design/README.md` (working guide; inventory generated from `manifests/`, never hand-edited) and `docs/design/authoring-kit/` (AUTHORING.md, R2-prefilled `package.yaml.template`, schema mirror stamped `.26`); and the three reconstruction overlays in `docs/design/manifests/` — interpretation placed before the operator once and whole. Measured identities: H-01 R0 `5de8520a…cd48` (72 files, 8,138,947 bytes), H-01 R1 as-reviewed `fd6141f6…3bef` (94 files, 12,886,907 bytes), H-01 R1 as-committed `b5664a2a…9a1d` (98 files, 13,058,284 bytes); the shipping tools reproduce the review round's ground truths exactly (path resolution 5/86/11/0; the `.9` delta as 7 changed + 4 added; ledger DEC-001…036 newest-first with the two backfilled status cells as the one permitted transition). **The instruments table is instituted below** with its first row, `{H-01, R1, as-committed}`; the R0 and as-reviewed rows arrive with their bytes (PR-B/PR-C, James-merged), as memo rows arrive with files. **Two operator rulings 2026-09-19, this session:** (i) the **import-lane boundary is ratified at the narrow scope** in the stewards' converged wording — *an import record is transcription and may ride the relayed-landings lane; a reconstruction is interpretation and may not*; in-lane only once the schema makes a relayed cut's products judgment-free (verbatim bytes, version bump, instruments row, machine-generated receipts and drift output); reconstructions, historical drift reports and all provenance/waiver judgment stay operator-merged, as do schema, tools and protocol; the three historical landings are James-merged; (ii) the **authoring kit relays to Claude Design ahead of the PR's merge**, draft-pending-merge — cut R2 is 25 Sep and the kit is the only artifact outside the repo on that path. **The PR #13 review round (ChatGPT/Codex + Claude Cowork, commit `4f208c4`) returned *changes requested* from both stewards, and both were right**: six enforcement defects, all of one class — guarantees stated in prose that the shipping validator did not impose — are repaired in the commit this note lands with, each reproduced first (memo §1a). Unvalidated nested `supersedes` values; null bypassing validation entirely, so "required" meant only "the key exists" and `mock_math: null` satisfied outcome 15; a tree digest blind to directory symlinks, so a package hidden behind one link digested identically to an empty directory; `package_path` typed against its own note; outcome 1 asserted with no pin validator; and an unrecognised `cut_state` silently disabling the one check that enforces the `.24` cut-key ruling. **The acceptance battery is now committed** — `tests/test_design_package.py`, 56 cases, runnable from a clean checkout in one command, building its R2 fixture from the committed package at run time. **A second re-review of the repairs (ChatGPT/Codex on `956b272`) found three more of the same class**, now also repaired: required nested records were structurally *hollow* (field names declared, no types — so a waiver naming no authority validated and the preflight honoured it; the contract said one thing and the validator accepted its opposite); rejected cuts resolved as valid pins (`resolve_pin` checked the triple but never the row's Status, Path or digest, so a bounced cut returned `ok` with an empty path, against outcomes 1 and 12); and **the corpus claim outran its committed evidence** — the memo said the full corpus was unchanged and that the result was a case in the battery, while the committed case checked six hand-picked memos for exit 0. That last one is this same failure in miniature and is repaired the strongest way available: `tests/fixtures/memo_preflight_pre_extraction.py` vendors the validator frozen at commit `5535428`, and the battery runs it against the live one over **all 27 memos** in `docs/correspondence/` — frozen and expected-to-fail material included — asserting identical exit codes and output, so the claim is re-provable on any future commit. Two items recorded rather than repaired: Cowork **withdrew** its independent verification of the digest spec (its reimplementation used `os.walk` too, so it reproduced the blind spot rather than testing around it — two implementations agreeing is not a spec compelling agreement); and the null-skip was **pre-existing**, inherited faithfully from `memo_preflight.py` at `.20`, so the fix changes the correspondence validator as well — the byte-identical corpus regression was therefore **deliberately re-baselined and produces no change on any memo**, and that result is itself a case in the battery. **A third re-review (Codex on `dfaaa0a`) found three more, again the same class**, repaired at the head this note lands with: the overlay's `evidence` map was unenforced because its value shape sat in an `x-value-shape` annotation the interpreter never read (`{source: null, strength: banana}` validated — outcome 10 was prose), so the interpreter now supports `additionalProperties` and the map declares an executable value schema; a *schema-valid* waiver could still name no authority (`authorized_by: ""`, `disposition: ""`, `date: not-a-date` all passed, since strings had no minimum length and `format: date` was decorative) — non-blank constraints and a date pattern are enforced now, and **the memo a waiver cites must itself resolve to an operative register row**; and a pin with a **blank** `Status` still resolved, because the lifecycle check fired only on a non-empty unrecognised value. Recorded as a correction, not a repair: the previous reply claimed the suite's `ResourceWarning`s were gone when 42 remained — only the two visible in the output actually read had been fixed. Every handle in `tools/` and `tests/` is now scoped, and the suite re-runs itself under `-W error::ResourceWarning` so the claim is checked rather than asserted. **A fourth round (Codex on `bcd01d5`) found the self-check itself vacuous**: the handle-hygiene case filtered the child run's suite by string match, which discarded the whole module, so the child exited 0 on `Ran 0 tests` while the parent asserted only the absence of a substring — a zero-test run reading as proof. The child now skips only that one case and runs the other 55, and the parent asserts the child's exit code and a positive test count; verified by mutation (reintroducing the filter fails with `0 not greater than 1: child ran no real tests`). Recorded because it is this round's lesson in one artifact: **the test written to stop a claim from rotting was itself a claim that had rotted** — evidence must be checked the way the thing it certifies is checked. A new cut state is coined in the register, never in a manifest: see the declared list below. Renumber-on-rebase enters the import checklist as ordinary procedure. Booklets pin the qualified triple from now on (`{design_package, revision, cut_state}` — the `.24` ruling); 023-R2's bare-pin prose is superseded in form, a forward rule since no booklet exists yet. Next free stays `027+`. 