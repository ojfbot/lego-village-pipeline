---
kind: migrated
note_sha256: 164a66c2999d3c73eaf66c2077968628b15f308e369fa7180fc4139481a59d7e
previous_version: 2026-09-18.27
register_version: 2026-09-18.28
slice: s-28
---
At `.28` adds the **CORR-LEGO-PIPE-028-R0 row (debrief on the design-handoff schema round, for review)** — number **proposed**, James confirms. **Landed as train car 2 at `.28`**, assigned by the merge-train shepherd against `main` `dd7f702` (register `.27`, CORR-027 landed); the memo's own `register_version_read` stays `.26`, the base it was reasoned on. 029–031 remain allocated and in flight; next free stays `032+`. Authored by Claude (Cowork) at operator request: a self-assessment of **this agent's** participation in the first complete propose → two independent reviews → reciprocal review → operator ruling → build → peer review then concurrence → repair to approval cycle (024-R0/R1, 025-R0/R1, 026-R0, PRs #10–#13). Reports four mechanisms that worked (independence before contact; verification over reading; a ruling that records its reason; withholding implementation to create the round), **six failures by its own author** (a digest verification that shared the implementation's blind spot and so proved nothing; a shallower implementation review than the peer's, having checked the claims rather than attacked the enforcement; a stale review body posted twice to PR #12; a commit amended after peers had cited it by hash; three hand-offs composed without re-reading remote state; register dialect used where plain words were asked for), and a **counted comparison of the memo surface against the pull-request surface**: across the round's four PRs, 10 of 10 agent reviews are authored by the operator's account, 5 of 10 carry a provenance block and all five appear only after operator intervention, four incompatible finding-id schemes were used, two byte-identical reviews stand undetected in PR #12, and the as-built memo can cite no pull-request-thread finding at all though four of the five defects that changed the implementation were raised there. **Recorded, not repaired:** four of the five PR #13 repair commits (`39ec8ce`…`41f0fb1`, replayed onto `main` by its rebase merge; `1f9b2d1` excepted) touched `docs/correspondence/` inside one unlanded PR without advancing this line — the standing rule speaks to landings and is silent on commits within one; put to the operator as Q-03 (per-commit or per-landing). Corrected at R0-c1 from an earlier "post-merge" wording, per the PR #14 shepherd review. Also recorded: the validator's reserved-namespace rule makes it impossible to cite a peer memo's finding by id without adopting it, a defect **demonstrated while writing this memo**: the first draft illustrated the proposed citation form with a real peer finding id and `memo_preflight.py` rejected it (*finding ids used but not declared*), so the example had to be abstracted for the memo to pass (Q-04). Ten requirements for a shared review schema are extracted for the separate session the operator has reserved for that design; this memo designs nothing. Five operator questions at §7. Passes `memo_preflight.py` (v2) exit 0 at authoring. 