# Routing sheet — who gets what, 2026-09-17

Rule from the register: **attach files, don't paste.** ChatGPT and Claude.ai accept `.md` attachments; Claude Code reads files from the repo, so commit them under `docs/correspondence/` and reference the path. Prefaces below are the whole message — short on purpose, because the memo's Orientation section does the explaining.

## 1. Claude Code — the work order (do this first)

**Files (commit to `ojfbot/lego-village-pipeline/docs/correspondence/`):**
- `LEGO-PIPE-011-from-studio-to-stage-R2.md` — the work order (Parts B–D are inside it)
- `REGISTER.md` → `docs/correspondence/REGISTER.md`
- `preflight.py` → `tools/preflight.py` (reference implementation to replace)
- `CORR-LEGO-PIPE-013-reconciliation.md` and `REVIEW-LEGO-PIPE-012-…md` — context only

**Preface:**
> New work order for `ojfbot/lego-village-pipeline`, play-well cluster: `docs/correspondence/LEGO-PIPE-011-from-studio-to-stage-R2.md`. Read its Orientation and §0a change log first; Parts B–D are the architecture, with ADR amendments appended at the end of Parts C and D (amendments win over earlier text). ADRs 0001–0005 from mils-integrator remain in force.
>
> Authorized scope, nothing beyond it: (1) the Stage-0 data contracts in §3 as typed schemas with validators, every ambiguity surfaced as a question for me before anything else is built; (2) `tools/preflight.py` replaced by a versioned `lego-pipe-memo/v1` JSON Schema + CLI, wired to `docs/correspondence/REGISTER.md`; (3) spike S2 in CI against OMR fixtures and the container half of S7; (4) `brickcore/` scaffolded only as far as S2/S7 need, moving mils-integrator's `ldraw/parser.py` and `catalog.py` rather than forking them; (5) create `ojfbot/play-well-library` empty with the attribution file and branch-flow CI rule. Register both repos in Northstar under play-well.
>
> Each spike ends in a findings note (markdown) in `docs/spikes/`. Open one issue per item in §6 "Open items". Do not scaffold production package boundaries until S1/S2/S7 have reported.

## 2. ChatGPT (build-harness thread) — for the record, no action

**File:** `CORR-LEGO-PIPE-013-reconciliation.md` (and `LEGO-PIPE-011-from-studio-to-stage-R2.md` if they want the final)

**Preface:**
> Closing the loop on LEGO-PIPE-012. Attached is CORR-LEGO-PIPE-013, the reconciliation: 22 findings accepted as written, 4 accepted with modification (R-10, R-16, R-20, R-25), none rejected. Your four clarifications from this morning are folded into 011-R2 as a "Clarifications R2" block; the preflight ran on that exact artifact and passed. 011-R2 has been dispatched to Claude Code for the limited scope you cleared (contracts, register/preflight, S1/S2/S7). No response needed unless you dispute anything in §B or §C of CORR-013.

## 3. Claude Design (design thread) — already has its brief

`design-review/HANDOFF-LEGO-PIPE-010-design-brief.md` v1.1 is issued. Nothing from the build-harness thread goes to Claude Design yet. One thing it may want later: the identity and export-manifest contracts from 011-R2 (so the drafting table's request/proposal cards carry `brick:id` and the three-band validation report), but that waits for Claude Code's Stage-0 schemas.

## 4. ChatGPT (design thread) — one item still returned to them

CORR-LEGO-PIPE-009 §3.1 asked ChatGPT whether `need_origin` can be derived from the parent Request/Design or must be stored per row. If unanswered, attach `CORR-LEGO-PIPE-009-reconciliation-response.md` and `HANDOFF-LEGO-PIPE-010-design-brief.md` with:
> D-1…D-5 are settled and HANDOFF-LEGO-PIPE-010 has gone to Claude Design (attached). One item from CORR-009 is still with you: §3.1, `need_origin` derived vs stored per row. A one-paragraph answer is enough.

## 5. Sol — audit of the correspondence system

**Files:** `REGISTER.md`, `preflight.py`, `CORR-LEGO-PIPE-009-…`, `HANDOFF-LEGO-PIPE-010-…`, `LEGO-PIPE-011-…-R2.md`, `REVIEW-LEGO-PIPE-012-…`, `CORR-LEGO-PIPE-013-…`

**Preface:**
> Sanity-check the inter-agent correspondence system for the Lego Village Pipeline, not the LEGO architecture itself. Two threads (design, build-harness) run across two providers (Claude, ChatGPT) with numbered memos. Attached: the register, the reference preflight, and the five most recent memos. Questions: (1) is the register's resolution of the 009 and 010 numbering collisions sound, and is one monotonic register with type prefixes the right fix; (2) do the two frontmatter styles in use (CORR-009's `document_id`/`from: {actor, role}` and 011-R2's `lego-pipe-memo/v1`) need unifying, and if so which wins; (3) does the disposition-ledger discipline (P0/P1/P2, `R-nn`, ACCEPT/ACCEPT-MOD/RETRACT/OPERATOR, "amendments appended, never rewritten") hold up across the chain, or are there places a decision was silently changed; (4) anything a future session would misread. Findings as a numbered list with severities.

## 6. Nobody — stays in the project only

`LEGO-PIPE-011-…-R0.md` and `-R1.md` (superseded), `claude-review-of-design-bundle-R0.md` (already reconciled), the four `mils-integrator/*` docs and `studio-bridge/research-…` (all embedded in 011-R2 as Parts B–D or cited by it). The two amended architecture docs (`build-harness/architecture-…` and `storage-…`) are canonical in the project; Claude Code gets them via 011-R2.

## 7. What I (Claude Cowork) own next, no paste needed

S1 (Studio round trip) and the native half of S7 on your Mac, then S6, S4, S5 in the R1 order. Say "start S1" when Studio is free.

## Note on the 010 collision

The design thread issued `HANDOFF-LEGO-PIPE-010` to Claude Design after the register had reserved 014 for it. Since a number that has reached an agent shouldn't move, the register now gives 010 to the design brief; the build-harness draft that Part C of 011 calls "LEGO-PIPE-010" was never sent standalone and is absorbed into 011 (cite it as "011 Part C"). Next free number is 014. The date context in that brief — demo 2026-12-25, hard gate 2026-10-21 for a trusted BOM, James sole user until the demo — is worth carrying into the build-harness thread: it puts phases 4–5 (family, kids) after the demo and makes S1/S2/S7 plus the BOM path the critical line.
