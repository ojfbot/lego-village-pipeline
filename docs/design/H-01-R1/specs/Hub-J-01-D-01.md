# Hub · J-01 · D-01

## Hub (`Hub.dc.html`)
Sheet index (cards with TYPE · GENERIC · FIDELITY rows; PLANNED cards dashed) and the **decision log** (newest first; each entry an accordion with inputs and blast radius: ⌕ trace chips filter the index, ▤ file chips preview a file inline). Behaviour to keep: tracing dims untouched sheets; previews fetch the file and show the head.

## J-01 Journey map (`J-01 Journey Map.dc.html`)
J1–J13 (+ O4) as a route with stops; each stop: plain-words title, steps with who does them, what it produces, gate, what must stay visible, which sheet instantiates it, open question. Filters by person and status. Statuses move as sheets land (P-01 and F-01 are built).

## D-01 Design library (`library/Design Library.dc.html`)
Canonical source for tokens (`dt/tokens.css`), primitives, domain components, states and the three motion moves (change pulse, stamp-in, snap pulse). Card behaviour spec in §02 (grow to cap → scroll → ⤢ expand). Open: animated grid reflow; iconography. Rejected: neumorphic surfaces (09-17).

### Domain components (build these in packages/ui)
RequestCard · StanceChip · OptionStateRow · PartPopover · BasisChip · PlantingRow · ChangePulse · DecisionLogEntry · ApprovalGate · EvidenceChips (◇ AI GUESS · ■ MACHINE-CHECKED · ■ CONFIRMED · ▲ CORRECTED · ○ ON HOLD) · EnvelopeBadge · StickerTile · SegmentedLine (SHOW/LOOK/ZOOM) · StatusLine (with undo) · ActionLog.
