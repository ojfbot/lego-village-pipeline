# Decision ledger

> Note (2026-09-17): the pre-014 rows below were numbered in place without editing their text; the `Supersedes` cell on those rows is `—` because supersession was not tracked before DEC-016.

Source: Hub decision log. Newest first. IDs are stable (`DEC-001` = oldest). Columns `supersedes` / `status` / `effective_revision` per HANDOFF-LEGO-PIPE-014 §2 — the ledger held 15 entries at numbering, not 13 as the brief estimated. `Touches` = blast radius at the time. Only the operator overturns an entry; overturning adds a new entry, never edits one.

| ID | When | Decision | Touches | Status | Effective revision | Supersedes |
|---|---|---|---|---|---|---|
| DEC-036 | 2026-09-17 22:00 | **Hub carries a "Right now" strip: three priorities, in order, each with its reason, one action, and two honest exits (done · not today).** No count of overdue, no red, no guilt copy; the lede changes with progress, never with delay. First instance of a type-generic priority surface; lifts to D-01 as PriorityStrip after real use. Seeded today: measure the tree base · order 10254 · confirm rail fit around the tree. | Hub, D-01 (pending), schema (Priority view) | accepted | A1 | — |
| DEC-035 | 2026-09-17 21:00 | **“Blocked by” is never a bare label.** Each blocker is a chip that previews the owning card and, where an action exists, clears the blocker from inside the popover; otherwise it lands on the card with the flash. Built as **Blocker** (D-01 §C6) on the shared overlay. | C-01 trail, then every gate/finding | accepted | R1 | — |
| DEC-034 | 2026-09-17 20:30 | **Procurement-release gate lives at the END of C-01, not the head of P-01.** Operator call after seeing it in place. Built as the reusable **ReleaseGate** (D-01 §C5); P-01 only reads the result. Supersedes DEC-025’s placement; the gate’s content is unchanged. | C-01, P-01, D-01 | accepted | R1 | DEC-025 (placement) |
| DEC-033 | 2026-09-17 19:40 | **Cross-sheet links preview, then flash where they land.** CrossRef chip (hover preview of the target card) + `dt/landing.js` (scroll to the card, `dtLand` flash, polite “Landed on”). Card ids sit on card containers. Overlay rule shared with SetRef. | D-01 §C4, tokens, every sheet | accepted | R1 | — |
| DEC-032 | 2026-09-17 19:10 | **Every set / MOC / part number is a SetRef.** Inline hover-or-focus card with image, name, held facts and links. Reference data from BrickLink, never LEGO.com. | D-01 §C3, M-01, C-01, then all sheets | accepted | R1 | — |
| DEC-031 | 2026-09-17 18:30 | **Reusable widgets are designed in D-01 first, then mounted.** D-01 re-cut as a ladder (A primitives → B patterns → C components → D sheets). First new component: **GapCloser** — a chip on any option with open strikes that lists the work to close them, dispatches agent runners (Blender → geometry check → Studio → parts research → fit proof) or marks a step by-hand, restates before running, and returns results as evidence — never as a decision. | D-01, C-01, schema WorkItem | accepted | R1 | — |
| DEC-030 | 2026-09-17 17:55 | **A-01 final layout: tabbed PLAN / 3D · CHECK IT above the canvas, with the small 3D ComponentViewer strip kept under the plan.** Both views on screen at once in plan mode; 3D tab enlarges the same renderer. Supersedes DEC-029 after the operator saw side-by-side. | A-01 | accepted | R1 | DEC-029 |
| DEC-029 | 2026-09-17 17:40 | **A-01 plan and 3D are on screen at once, side by side, equal size.** Overrides the "tabbed" layout in DEC-018 after the operator saw it. The 3D view keeps following the layer rig and separation depth. | A-01 | superseded by DEC-030 | R1 | DEC-018 (layout only) |
| DEC-028 | 2026-09-17 16:20 | **Manifest is the single source of status.** `handoff/index.json` gains `path_base`, decision records and schema requests inline; J-01, Hub and H-01 read it; a cross-artifact check (`dt/check-contrast.mjs`, manifest resolver) fails drift instead of shipping it. Cites HANDOFF-LEGO-PIPE-014 §2. | index.json, J-01, Hub, H-01 | accepted | R1 | — |
| DEC-027 | 2026-09-17 16:18 | **Family identities are initials — EH, HH, LH.** Replaces `@boys`. Authorship, consent and replies attributable per person; no fuller detail exposed in-app. Cites 014 §9. | F-01, Request.from, stances | accepted | R1 | — |
| DEC-026 | 2026-09-17 16:16 | **Measurement + verify-a-claim loops get one sheet: M-01 "Measure and check."** Linked from A-01 and C-01; every entry returns to the exact interrupted spot. Cites 014 §3.3, §10.2. | new M-01, A-01, C-01 | accepted | R1 | — |
| DEC-025 | 2026-09-17 16:14 | **Procurement-release gate lives at the head of P-01.** You pass the gate before seeing shops. Formal gate record per 014 §8; quiet modal + stamp on the surface. A retroactively entered manual order is a supported, first-class case. | P-01, C-01 exit, GateRecord | placement superseded by DEC-034 | R1 | DEC-008 (ladder rung 3 placement) |
| DEC-024 | 2026-09-17 16:12 | **"Rail" is the F-01 string until RESEARCH-01 lands.** All readouts change to match ("overlaps the rail", "beside the rail"). Cites 014 §2. | F-01 copy | accepted | R1 | — |
| DEC-023 | 2026-09-17 16:10 | **Contrast fix: darken base values; `--dt-unres` is a text token.** Paper: accent #B84F2B, prov #8F660E, unres #5C6E82; blueprint unres #8598AC. `-deep` variants darkened to keep hierarchy. Contract + `check-contrast.mjs` added to the token file. Cites 014 §2. | dt/tokens.css, every sheet | accepted | R1 | DEC-011 (token values only) |
| DEC-022 | 2026-09-17 16:08 | **K-7 proportionality.** Where rigor and the frictionless-teacher principle conflict on a family surface, the teacher wins and the rigor moves to the operator side. F-01 "choose evidence depth" becomes one plain question ("a guess, or did you check?"). Cites 014 K-7, §5A. | F-01, Request↔GateRecord join | accepted | R1 | — |
| DEC-021 | 2026-09-17 16:06 | **K-6 accessibility contract is acceptance-gating.** Full semantic retrofit of A-01, C-01, P-01 first (h1, landmarks, lang, title, names, live regions, keyboard, focus), a per-sheet announcement spec and an accessibility-tree snapshot per sheet. Cites 014 §6. | A-01, C-01, P-01, then all sheets | accepted | R1 | — |
| DEC-020 | 2026-09-17 16:04 | **K-5 / K-8 / K-9 work order.** Order-by gate 2026-10-21 is the deadline, not the demo. Tier 1 = Journeys 2–5, geometry + BOM correctness, procurement gate. One user (James) until the demo; F-01 responsive leaves the critical path. BOM correctness outranks procurement optimisation; the record outranks the automation. Cites 014 §3.4. | sequencing of every sheet | accepted | R1 | — |
| DEC-019 | 2026-09-17 16:02 | **K-4 Frame topology and ledger ownership are operator decisions, unadjudicated.** `repo-structure.md` two-app split marked as such; `seam.md` frozen as assumptions. Claude Code may evidence and recommend; no settling by scaffolding. Cites 014 K-4. | repo-structure.md, seam.md | accepted | R1 | — |
| DEC-018 | 2026-09-17 16:00 | **K-3 A-01 3D viewer is preview + verification surface.** Same canonical geometry as the 2D canvas; lo-fi massing, truthful dimensions; transforms propose, never persist (reuses dashed-proposed pattern). Layout: tabbed — the viewer takes the full canvas area when active. *(Layout superseded by DEC-029.)* Viewer/canvas *split* stays deferred (Q3). Cites 014 §4. | A-01, geometry schema request | accepted | R1 | — |
| DEC-017 | 2026-09-17 15:58 | **K-2 MILS modules are 32×32 (16×32, 8×16 intermediates).** Curved track rides on a *group* of standard modules acting as one substrate. No 48×48 class. Placeholder for the group: **"Section"** until RESEARCH-01. Cites 014 K-2. | A-01 labels, RESEARCH-01, schema | accepted | R1 | — |
| DEC-016 | 2026-09-17 15:56 | **K-1 Tree 41843 measured:** base 16×16 studs · height 40 bricks · base height 8 bricks · branch overhang r28 studs · clear height under branches 10 bricks. F-01 Tweaks loses the tree placeholders; fit checks run against the real obstacle. Supersedes the "to be measured" fact below and F-01's 32×32 default. Cites 014 K-1. | F-01, A-01 obstacle, C-01 fit | accepted | R1 | DEC-003 (tree defaults) |
| DEC-015 | 2026-09-17 14:05 | **Research note filed: decomposition vocabulary.** "8 parts, 288 pieces" exposed placeholders. A research agent settles LEGO/AFOL/BrickLink/MILS terms per scale, axis and audience; glossary + lint rules; calls on Rail/Ring and unit/module/quadrant. P-01 states it only buys what C-01 approved; recorded orders flow back to inventory. | every sheet (copy), schema naming, brief/RESEARCH-01 | — | accepted | R0 |
| DEC-014 | 2026-09-17 12:10 | **Schema: sticker → envelope → model.** A sticker is the idea; it claims an envelope (footprint in studs × clear height at that spot — less under the tree, none on the base or ring); a model fills it and must fit. Applies to operator sheets too: every component has an envelope; fit checks compare model to envelope. Naming open: F-01's layer toggle says "Rail" (was "Ring") as a placeholder. A neumorphic surface pass was tried on F-01 and rejected (too glossy); reverted to flat sheet + hairline + vapor shadow. | F-01, A-01 component envelope, schema Envelope/Model | — | accepted | R0 |
| DEC-013 | 2026-09-17 09:20 | **P-01 drawn; F-01 reworked.** Procurement prices every shop combination (parts + postage + minimum shortfall), three priorities with outcomes shown before picking, drop-cost per shop, orders freeze the plan. F-01 renamed Family Workbench (no talking-down); stickers claim real studs on a 112×112 table; "Something else" makes a sticker from words; step 4 Model it (Studio file · photos · wand mock). A-01 layer rig scoped per MILS unit. | P-01, F-01, A-01, 2 schema requests | — | accepted | R0 |
| DEC-012 | 2026-09-16 13:41 | **C-01 landed; follow-up: split A-01, add source facets.** A-01 does double duty (component viewer + layout canvas). The viewer must also inspect by design source (BrickLink design · our own · official) and brick source (in inventory · arrives with a set · buy individually) — same basis vocabulary as C-01. | A-01, library basis vocabulary, schema Part.basis / Design.source | — | accepted | R0 |
| DEC-011 | 2026-09-16 13:24 | **C-01 corrected to real sources; basis is first-class.** Train (LEGO direct) = full circle of 16 curves, owned on arrival. bl-741807 = MILS frame + track attachment, bought with colours adapted. Ballast = research choice. Every row/element/link carries basis and vendor; part popover; options show modeled/fit/stances; requests show attached work. | C-01, A-01 mock note, 5 schema requests | — | accepted | R0 |
| DEC-010 | 2026-09-16 12:58 | **Prices are ranges; procurement is its own sheet (P-01).** Low–high across lots; vendor-smart ordering separated. One uniform change pulse on every value change. | C-01, P-01, tokens (change pulse) | — | accepted | R0 |
| DEC-009 | 2026-09-16 12:31 | **Requests are first-class; cards fixed-height with expand.** Any "+1" surfaces until answered — decline / park / accept with reasons and reply; nothing deleted. Cards fill their row, scroll inside, expand while neighbours collapse. | C-01, library §02, all future sheets | — | accepted | R0 |
| DEC-008 | 2026-09-16 12:04 | **Approval ladder.** No gate → reviewed diff → initials + stamp → purchase outside the app. Configurable per list; operator may override. | C-01, H-01 schema, all future | — | accepted | R0 |
| DEC-007 | 2026-09-16 11:36 | **Principle: every UI is a frictionless teacher.** Plain words first; controls explain consequences; AI questions as conversation (tap/type/talk); no prepositional jargon in titles. | B-01 rebuilt + renamed, CLAUDE.md, all future sheets | — | accepted | R0 |
| DEC-006 | 2026-09-16 11:14 | **Vapor shadows + LEGO radius grammar.** Two-layer offset shadows; cards get three fillets and one stud corner; circles reserved for gauges. | every sheet (token level) | — | accepted | R0 |
| DEC-005 | 2026-09-16 10:48 | **Z axis: whole/fraction modes, plate/brick units.** LDU is the integer base; whole mode = stud XY, 22.5° steps, Z in plates or bricks; fraction mode behind a toggle. Labels follow the chosen unit. | A-01 transform card, library §03, 2 schema requests | — | accepted | R0 |
| DEC-004 | 2026-09-16 10:26 | **Layer rig: scene tree + separation peel; brick-level accordion planned.** Semantic scene layers and physical separation depth kept as separate axes; both must accordion to brick level; separation strata substrate for derived build order. | A-01, 3 schema requests, future build-order flow | — | accepted | R0 |
| DEC-003 | 2026-09-16 10:05 | **Gates: stamps for the record, quiet modals for the act.** | C-01, A-01 save vs freeze | — | accepted | R0 |
| DEC-002 | 2026-09-16 09:41 | **Family posture: Family Workbench; stories fork the twin.** Same tools at tablet density; a proposed scene = layout branch with origin family · workflow proposed and deterministic consequences. | F-01, J11, evidence model | — | accepted | R0 |
| DEC-001 | 2026-09-16 09:12 | **Direction: Drafting Table (paper / blueprint); Organic scrapped.** Chosen from three built candidates; one shared token file. | every sheet, dt/tokens.css | — | accepted | R0 |

## Corrected domain facts (seed every implementation)
- 10254 Winter Holiday Train: full circle of track only (16 × 53400 curves), no straights, bought from LEGO direct, owned on arrival, earmarked for the railbed.
- bl-741807 MILS quarter circle R40: Technic landscape frame + track-attachment pieces, ×4 to carry the circle, not ballast; parts cannot be bought as listed — colours adapt to the winter palette; every adapted row records colour_source → colour_chosen.
- Ballast (single/double plate under rail) is a research choice, in neither source.
- Family Christmas Tree 41843 is owned and built; its base is a fixed obstacle. Base size, height, base height and branch overhang **were measured 2026-09-17 (DEC-016): 16×16 studs · 40 bricks · 8 bricks · radius 28 studs · 10 bricks clear.** The earlier "to be measured" line and F-01's 32-stud default are superseded, not edited.
