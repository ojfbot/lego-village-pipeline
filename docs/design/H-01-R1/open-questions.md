# Open questions

| # | Question | Owner | Surfaces on |
|---|---|---|---|
| Q1 | **RESEARCH-01 — decomposition vocabulary.** Exact LEGO / AFOL / BrickLink / MILS terms per scale and audience; glossary + lint rules; calls on "Rail vs Ring" and "unit / module / quadrant". See `../brief/RESEARCH-01-nomenclature.md`. | research agent | every sheet; schema names |
| Q2 | ~~**Tree 41843 measurements**~~ **CLOSED — DEC-016:** 16×16 st · 40 br · 8 br base · r28 st overhang · 10 br clear. | operator | F-01, A-01 |
| Q3 | **A-01 split** — deferred by decision (DEC-018); the 3D viewer expands *inside* A-01 as a tabbed verification surface (Tier 1). The split is specced or explicitly deferred in Tier 3 (014 §10.13). | design session | A-01 |
| Q4 | **Card reflow** — full animated grid reflow when a card expands and neighbours collapse. | design system session | C-01, library §02 |
| Q5 | **Wand backend** — retrieval over precedent models (story + claimed envelope + kind → precedents → proposal), audited. Mock today. | Claude Code + research | F-01 step 4 |
| Q6 | **Procurement data** — real BrickLink price-guide / store fetch, account history → VendorProfile. Mock today (5 shops). | Claude Code | P-01 |
| Q7 | **Responsive design for iPhone and iPad** — not done; only intent is specced. Needs its own design cycles. | design session | all sheets, F-01 first |
| Q8 | **Frame seam** — see `seam.md` questions. | Frame team | Hub, all |
| Q9 | **Rights review flow** — B-01 shows the listing-visibility control; the artifact-vs-listing split for family visibility needs a policy record. | operator | B-01, F-01 |
| Q10 | **Build order** as a derived view from separation strata (not the manual peel). | design session | A-01 |
| Q11 | **`need_origin` row-level or parent-level?** A part serving two needs. Returned to ChatGPT; UI must not assume either. | ChatGPT (schema review) | C-01 |
| Q12 | **Three.js double import** in A-01 — locate the second instance (bundler vs helmet script). | design session (WP0d) | A-01 |
| Q13 | **Fit proof inputs beyond the tree** — track circle geometry (16 × 53400, R40 centre-line), Section arrangement for the curve, MILS height under the base. | operator + RESEARCH-01 | A-01, C-01, M-01 |
| OW-01 | **D-01 lift** — components and patterns that live only on sheets get drawn in the library, folded in as each sheet is touched (running to-do; goes to the design-audit open-work ledger at the end of this cycle). | design session | D-01, every sheet |
