# Reconstruction overlay — the COMMITTED H-01 R1 tree (the as-committed received state).
# NOT a manifest: the designer did not export these exact bytes as one act — this is the
# as-reviewed cut plus the operator-requested ADDENDUM-A1 and three hub captures, landed
# by ruling at register .9. See design-package-overlay.v1.schema.json.
overlay_schema: design-package-overlay/v1
design_package: H-01
revision: R1
cut_state: as-committed
package_path: docs/design/H-01-R1
tree_sha256: b5664a2ad0ed7b7c7f093f4a1b369b32095799f73ade2af074b882e8038b9a1d
manifest_origin: importer_reconstructed
governing_brief: HANDOFF-LEGO-PIPE-014-R0
answers: []
supersedes:
  design_package: H-01
  revision: R1        # the as-reviewed received state of the same revision; the digest disambiguates
  tree_sha256: fd6141f6f36d4f8852ceedc1fa5dc7306a12ffb507ced462bab99c1999a83bef
maturity: prototype
tier_coverage: tier-1
mock_math: true
path_prefix_strip: "handoff/"   # the rule for 86 of 102 structured index paths (REVIEW-025 §2)
archive_sha256_claimed: e5b4857c4f4ed222fee496b1852285d329410ff5ed1023d2a8fbde5f8a6270ce
archive_digest_status: claimed  # verified at import (register .9, LATEST.zip, 10,013,923 bytes); the archive is gone, so today it is a preserved claim
decisions:
  ledger: decisions.md
  last_id: DEC-036
  count: 36
fixtures: null                   # fixture seeding starts at cut R2 (023-R2 F5)
known_defects:
  - id: D-1
    rule_id: contrast-contract
    paths: ["dt/tokens.css"]
    expected_failure: "node dt/check-contrast.mjs → FAIL … 1 contrast failure(s), exit 1 (blueprint --dt-block 4.27:1, needs 4.5)"
    evidence: HANDOFF-LEGO-PIPE-019-R0 §4.2
    status: open
  - id: D-2
    rule_id: package-self-check
    paths: ["check-manifest.mjs", "screenshots/STATES.md"]
    expected_failure: "node check-manifest.mjs → MISSING + drift lines + uncaught ENOENT on .dc.html sources; also nondeterministic (compares mtimes git does not preserve)"
    evidence: HANDOFF-LEGO-PIPE-019-R0 §4.2; REVIEW-LEGO-PIPE-025-R1 §2
    status: partially closed — the four-capture STATES.md mismatch closed by the .9 superset; the .dc.html half is reclassified by 024-R1 as design_source references, not a defect
  - id: D-3
    rule_id: offline-render
    paths: ["standalone/A-01 Layout canvas.html"]
    expected_failure: "A-01 loads Three.js from a CDN; offline canvas: 0 — the README's 'no server' claim is false for A-01"
    evidence: HANDOFF-LEGO-PIPE-019-R0 §4.2
    status: open
  - id: D-4
    rule_id: path-base
    paths: ["index.json"]
    expected_failure: "path_base describes dt/, handoff/, brief/, prototypes/; the archive shipped only handoff/ — 86 of 102 structured paths need the handoff/ prefix stripped"
    evidence: HANDOFF-LEGO-PIPE-019-R0 §4.2; REVIEW-LEGO-PIPE-025-R1 §2
    status: closed by contract from cut R2 (design-package/v1 path_base '.'); historical trees carry this declared path_prefix_strip instead
  - id: D-5
    rule_id: a11y-landmarks
    paths: ["standalone/"]
    expected_failure: "landmarks thin outside A-01/C-01/P-01: no h1/main on five sheets, no banner/nav/contentinfo where role=main exists, no live region on Hub/J-01"
    evidence: HANDOFF-LEGO-PIPE-019-R0 §4.2
    status: open
  - id: D-6
    rule_id: file-fetch
    paths: ["standalone/H-01 Handoff.html"]
    expected_failure: "inline file preview fetch()es a file:// URL, which browsers refuse"
    evidence: HANDOFF-LEGO-PIPE-019-R0 §4.2
    status: open
reconstruction:
  authored_by: Claude Code
  date: 2026-09-19
  sources:
    - "register .9 note (import ruling: LATEST.zip = as-reviewed cut + ADDENDUM-A1 + three hub captures)"
    - "HANDOFF-LEGO-PIPE-019-R0 §4.2 (defect ids D-1…D-6 — a memo, not the package: the package's own known_defects_open carries no ids)"
    - "HANDOFF-LEGO-PIPE-014-R0 register row (executed by Claude Design as H-01 R1)"
    - "docs/design/H-01-R1/ bytes (measured in-repo)"
    - "HANDOFF-LEGO-PIPE-024-R1 (this protocol; cut-state vocabulary)"
  corroboration: >
    Tree digest, byte and file counts recomputed from the committed bytes (measured).
    The archive hash is the register .9 record, verified at import time and unverifiable
    today — the zip no longer exists; preserved as a claim, never as prose in a digest
    field. 98 files = the as-reviewed 94 plus ADDENDUM-A1-right-now.md and three
    screenshots/states/1[45]-hub-right-now-*.jpg, matching the .9 note exactly
    (drift-H-01-R1-reviewed-to-H-01-R1.md is the mechanical record).
evidence:
  tree_sha256: {source: "tools/design_pkg.py digest", strength: measured}
  governing_brief: {source: "014-R0 register row", strength: declared}
  supersedes: {source: "register .9 note + measured digests of both trees", strength: measured}
  maturity: {source: "019-R0 §4 readiness verdict", strength: declared}
  tier_coverage: {source: "019-R0 §4 readiness verdict", strength: declared}
  mock_math: {source: "019-R0 §8 / package README", strength: declared}
  decisions: {source: "parsed docs/design/H-01-R1/decisions.md", strength: measured}
  archive_sha256_claimed: {source: "register .9 note", strength: declared}
  known_defects: {source: "019-R0 §4.2", strength: declared}
